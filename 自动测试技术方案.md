# 医院体检报告管理系统 — Playwright AI 自动化测试技术方案

---

## 1. 项目概述

### 1.1 项目背景

本项目为**医院体检报告管理系统**提供端到端（E2E）自动化测试解决方案，采用 **Playwright + TypeScript + AI 辅助**技术栈，实现体检报告全生命周期的自动化测试覆盖。

### 1.2 自动化测试目标

| 目标 | 说明 |
|------|------|
| 核心流程零漏测 | 体检报告 创建→编辑→提交→审核→发布→查看 全链路自动化覆盖 |
| 冒烟测试 ≤5min | 每次部署后 5 分钟内完成核心功能可用性验证 |
| 回归测试 ≤30min | 每次迭代发布前 30 分钟内完成全量回归 |
| CI/CD 门禁 | 自动化测试作为流水线质量门禁，阻断质量不达标的发布 |

### 1.3 测试覆盖范围

| 层级 | 覆盖内容 |
|------|----------|
| **P0 冒烟** | 登录、报告列表加载、报告详情查看、报告编辑保存、提交审核、审核通过 |
| **P1 回归** | 报告 CRUD、状态流转、角色权限控制、查询过滤、报告发布与查看 |
| **P2 扩展** | 边界值输入、并发编辑冲突、大数据量分页 |

---

## 2. 技术架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    CI/CD 流水线                              │
│  GitLab CI / Jenkins → Docker → Playwright 执行环境          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  Playwright 测试框架                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ 测试用例层   │  │ Page Object │  │    Fixtures 层      │  │
│  │ tests/      │  │ pages/      │  │  fixtures/          │  │
│  │ • smoke/    │  │ • login.page│  │  • auth.fixture     │  │
│  │ • regression│  │ • report-*  │  │  • data.fixture     │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │ 工具层       │  │ 测试数据     │  │    AI 辅助模块       │  │
│  │ utils/      │  │ data/       │  │  ai/                │  │
│  │ • api-helper│  │ • users.json│  │  • test-generator   │  │
│  │ • db-helper │  │ • reports   │  │  • failure-analyzer │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    被测系统 (SUT)                            │
│  医院体检报告管理系统 (Vue2 + Spring Boot + MySQL)           │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 类别 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| **编程语言** | TypeScript | ^5.3.0 | 类型安全、IDE 智能提示、团队维护成本低 |
| **测试框架** | @playwright/test | ^1.50.0 | 内置 test runner，原生支持并发、fixture、reporter |
| **浏览器** | Chromium / Firefox | - | 覆盖主流浏览器内核 |
| **包管理器** | pnpm | - | 安装速度快、磁盘占用小 |
| **Node.js** | LTS | ≥18 | Playwright 最低要求 |
| **报告工具** | Allure / HTML Reporter | ^3.0.0 | 美观的报告展示、历史趋势分析 |

### 2.3 项目目录结构

```
e2e-tests/
├── playwright.config.ts           # Playwright 全局配置
├── package.json                   # 依赖管理
├── tsconfig.json                  # TypeScript 配置
├── .env / .env.example            # 环境变量配置
├── .gitlab-ci.yml                 # GitLab CI 流水线
├── Jenkinsfile                    # Jenkins 流水线
│
├── tests/                         # 测试用例
│   ├── smoke/                     #   冒烟测试 (P0)
│   │   ├── login.spec.ts
│   │   ├── report-list.spec.ts
│   │   ├── report-view.spec.ts
│   │   ├── report-edit.spec.ts
│   │   └── report-audit.spec.ts
│   │
│   └── regression/                #   回归测试 (P1)
│       ├── report-crud.spec.ts
│       ├── report-workflow.spec.ts
│       ├── report-query.spec.ts
│       ├── permission.spec.ts
│       └── report-publish.spec.ts
│
├── pages/                         # Page Object
│   ├── login.page.ts
│   ├── report-list.page.ts
│   ├── report-detail.page.ts
│   ├── report-edit.page.ts
│   └── audit.page.ts
│
├── fixtures/                      # 测试夹具
│   ├── auth.setup.ts              #   登录态准备
│   ├── auth.fixture.ts            #   角色 fixture
│   └── data.fixture.ts            #   数据准备
│
├── utils/                         # 工具函数
│   ├── api-helper.ts              #   API 调用辅助
│   ├── db-helper.ts               #   数据库操作
│   └── wait-helper.ts             #   智能等待策略
│
├── data/                          # 测试数据
│   ├── users.json                 #   用户角色数据
│   └── reports.json               #   报告模板数据
│
├── ai/                            # AI 辅助模块
│   ├── test-generator.ts          #   用例生成
│   ├── script-generator.ts        #   脚本生成
│   ├── failure-analyzer.ts        #   失败分析
│   └── locator-healer.ts          #   定位器自愈
│
└── dashboard/                     # AI 测试管理界面
    └── server/
        └── index.ts               #   本地 Web UI 服务
```

---

## 3. 核心设计

### 3.1 Page Object Model 设计

**设计原则：**
- 每个页面一个 PO 类
- PO 只封装**定位器**和**操作方法**，不包含断言
- 断言在测试用例中编写，保持 PO 的复用性

**页面元素定位策略（优先级从高到低）：**

| 优先级 | 定位方式 | 示例 | 适用场景 |
|--------|---------|------|---------|
| 1 | `data-testid` | `[data-testid="btn-submit"]` | 首选，不受 UI 重构影响 |
| 2 | `aria-label` / Role | `getByRole('button', { name: '提交' })` | 语义化元素 |
| 3 | 文本内容 | `getByText('保存成功')` | 提示信息、按钮文字 |
| 4 | 稳定 CSS 选择器 | `.report-table .status-tag` | 以上方式均不可用时 |

**Page Object 示例：**

```typescript
// pages/report-edit.page.ts
import { type Page, type Locator } from '@playwright/test';

export class ReportEditPage {
  readonly page: Page;
  readonly patientNameInput: Locator;
  readonly bloodPressureInput: Locator;
  readonly saveButton: Locator;
  readonly submitAuditButton: Locator;
  readonly successToast: Locator;

  constructor(page: Page) {
    this.page = page;
    this.patientNameInput = page.getByTestId('input-patient-name');
    this.bloodPressureInput = page.getByTestId('input-blood-pressure');
    this.saveButton = page.getByTestId('btn-save');
    this.submitAuditButton = page.getByTestId('btn-submit-audit');
    this.successToast = page.getByText('保存成功');
  }

  async goto(reportId: string) {
    await this.page.goto(`/reports/${reportId}/edit`);
  }

  async fillExamResults(data: { bloodPressure?: string; comment?: string }) {
    if (data.bloodPressure) {
      await this.bloodPressureInput.fill(data.bloodPressure);
    }
    if (data.comment) {
      await this.page.getByTestId('input-comment').fill(data.comment);
    }
  }

  async save() {
    await this.saveButton.click();
    await this.successToast.waitFor({ state: 'visible' });
  }

  async submitForAudit() {
    await this.submitAuditButton.click();
    await this.page.getByRole('button', { name: '确认' }).click();
  }
}
```

### 3.2 登录态管理

采用 Playwright 原生 **storageState** 机制，一次登录、全局复用：

```typescript
// fixtures/auth.setup.ts
import { test as setup } from '@playwright/test';

const users = {
  doctor: { username: 'doctor01', password: 'Test@1234' },
  auditor: { username: 'auditor01', password: 'Test@1234' },
  admin: { username: 'admin01', password: 'Test@1234' },
};

for (const [role, cred] of Object.entries(users)) {
  setup(`authenticate as ${role}`, async ({ page }) => {
    await page.goto('/login');
    await page.getByTestId('input-username').fill(cred.username);
    await page.getByTestId('input-password').fill(cred.password);
    await page.getByTestId('btn-login').click();
    await page.waitForURL('/dashboard');
    await page.context().storageState({
      path: `./fixtures/.auth/${role}.json`,
    });
  });
}
```

```typescript
// fixtures/auth.fixture.ts
import { test as base } from '@playwright/test';

type RoleFixtures = {
  doctorPage: Page;
  auditorPage: Page;
  adminPage: Page;
};

export const test = base.extend<RoleFixtures>({
  doctorPage: async ({ browser }, use) => {
    const ctx = await browser.newContext({
      storageState: './fixtures/.auth/doctor.json',
    });
    const page = await ctx.newPage();
    await use(page);
    await ctx.close();
  },
  auditorPage: async ({ browser }, use) => {
    const ctx = await browser.newContext({
      storageState: './fixtures/.auth/auditor.json',
    });
    const page = await ctx.newPage();
    await use(page);
    await ctx.close();
  },
});
```

### 3.3 测试用例示例

```typescript
// tests/smoke/report-edit.spec.ts
import { test, expect } from '../../fixtures/auth.fixture';
import { ReportEditPage } from '../../pages/report-edit.page';
import { createTestReport } from '../../utils/api-helper';

test.describe('冒烟测试 - 报告编辑保存', () => {
  let reportId: string;

  test.beforeEach(async ({ doctorPage }) => {
    reportId = await createTestReport({
      patientName: '冒烟测试患者',
      status: 'draft',
    });
  });

  test('医生编辑体检结果并保存', async ({ doctorPage }) => {
    const editPage = new ReportEditPage(doctorPage);
    await editPage.goto(reportId);

    await editPage.fillExamResults({
      bloodPressure: '120/80',
      comment: '各项指标正常',
    });
    await editPage.save();

    await expect(editPage.successToast).toBeVisible();
  });
});
```

---

## 4. AI 辅助测试能力

### 4.1 AI 能力矩阵

| 能力 | 输入 | 输出 | 实现方式 |
|------|------|------|---------|
| **AI 生成测试用例** | 需求文档 / 页面截图 | 结构化测试用例列表 | LLM Prompt + 模板 |
| **AI 生成 Playwright 脚本** | 测试用例描述 + PO 接口 | 可执行的 .spec.ts 文件 | LLM Prompt + 代码模板 |
| **AI 自动修复定位器** | 失败截图 + 旧定位器 + 页面 DOM | 修复后的定位器 | LLM 分析 DOM 变更 |
| **AI 分析失败日志** | 错误日志 + 截图 + trace | 失败根因分析报告 | LLM 日志分析 |

### 4.2 AI 模块架构

```
┌─────────────────────────────────────────────────────────────┐
│                      AI 测试流水线                           │
│                                                             │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌───────┐ │
│  │ 上下文   │ →  │ LLM 客户端│ →  │ 后处理器  │ →  │ 输出  │ │
│  │ 收集器    │    │          │    │          │    │       │ │
│  └──────────┘    └──────────┘    └──────────┘    └───────┘ │
│       │                │                │                   │
│       ▼                ▼                ▼                   │
│  • 页面 DOM 分析    • OpenAI API    • 代码格式化          │
│  • PO 接口提取     • 流式响应      • 导入语句注入          │
│  • 测试数据准备    • 错误重试      • 断言优化              │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 AI 模块核心文件

| 文件 | 职责 |
|------|------|
| `ai/llm-client.ts` | LLM API 客户端封装（OpenAI/本地模型） |
| `ai/context-collector.ts` | 收集页面 DOM、PO 接口等上下文信息 |
| `ai/test-generator.ts` | 根据需求生成测试用例 |
| `ai/script-generator.ts` | 根据用例生成 Playwright 脚本 |
| `ai/failure-analyzer.ts` | 分析失败日志，给出根因和修复建议 |
| `ai/locator-healer.ts` | 自动修复失效的定位器 |
| `ai/pipeline-logger.ts` | 结构化日志记录 |
| `ai/cli.ts` | 命令行入口 |

### 4.4 AI 工作流程

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  需求/页面变更  │────→│  AI 分析变更   │────→│  生成/更新用例  │
└──────────────┘     └──────────────┘     └──────────────┘
                                                │
                                                ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  人工 Review   │◄────│  AI 生成脚本   │◄────│  确认用例      │
└──────────────┘     └──────────────┘     └──────────────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  CI 执行测试   │────→│  失败用例      │────→│  AI 分析失败   │
└──────────────┘     └──────────────┘     └──────────────┘
                                                │
                                                ▼
                                          ┌──────────────┐
                                          │  AI 修复定位器  │
                                          │  或提交 Bug    │
                                          └──────────────┘
```

---

## 5. 测试执行策略

### 5.1 并发执行策略

| 场景 | Workers 数 | 说明 |
|------|-----------|------|
| CI 冒烟 | 4 | 快速并行，5 分钟内完成 |
| CI 回归 | 4 | 按模块并行，用例间数据隔离 |
| 本地调试 | 1 | 串行执行，方便断点 |


### 5.2 浏览器支持策略

| 环境 | 浏览器 | 说明 |
|------|--------|------|
| CI 冒烟测试 | Chromium only | 最快速度完成核心验证 |
| CI 回归测试 | Chromium + Firefox | 覆盖主流内核 |
| 本地开发调试 | Chromium（headed） | 可视化调试 |

### 5.3 配置示例 (playwright.config.ts)

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
  timeout: 60_000,
  expect: { timeout: 10_000 },
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: 1,
  reporter: process.env.CI
    ? [
        ['html', { open: 'never' }],
        ['junit', { outputFile: 'results/junit.xml' }],
        ['allure-playwright'],
      ]
    : [['html', { open: 'on-failure' }]],

  use: {
    baseURL: process.env.BASE_URL || 'http://localhost:8080',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
    trace: 'retain-on-failure',
  },

  projects: [
    {
      name: 'smoke-chromium',
      testMatch: /smoke\/.*\.spec\.ts/,
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'regression-chromium',
      testMatch: /regression\/.*\.spec\.ts/,
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'regression-firefox',
      testMatch: /regression\/.*\.spec\.ts/,
      use: { ...devices['Desktop Firefox'] },
    },
  ],
});
```

---

## 6. CI/CD 集成

### 6.1 流水线设计

```
代码提交 (git push)
  │
  ▼
┌─────────────────────────────────┐
│  阶段 1：构建部署                 │
│  - 后端 mvn package              │
│  - 前端 npm run build            │
│  - 部署到测试环境                 │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│  阶段 2：冒烟测试                 │
│  - npx playwright test           │
│    --project smoke-chromium      │
│  - 失败 → 阻断流水线 & 通知       │
│  - 通过 → 继续                    │
└─────────────┬───────────────────┘
              │（仅 merge 到 main 分支触发）
              ▼
┌─────────────────────────────────┐
│  阶段 3：回归测试                 │
│  - npx playwright test           │
│    --project regression-*        │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│  阶段 4：报告归档与通知           │
│  - 上传 HTML Report 到制品库      │
│  - 发送钉钉/邮件通知          │
└─────────────────────────────────┘
```

### 6.2 GitLab CI 配置

```yaml
stages:
  - build
  - deploy-test
  - smoke-test
  - regression-test
  - report

smoke-test:
  stage: smoke-test
  image: mcr.microsoft.com/playwright:v1.50.0-jammy
  script:
    - cd e2e-tests
    - pnpm install --frozen-lockfile
    - npx playwright test --project=smoke-chromium
  artifacts:
    when: always
    paths:
      - e2e-tests/playwright-report/
      - e2e-tests/test-results/
    expire_in: 7 days
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push"'

regression-test:
  stage: regression-test
  image: mcr.microsoft.com/playwright:v1.50.0-jammy
  script:
    - cd e2e-tests
    - npx playwright test --project=regression-chromium --project=regression-firefox
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  allow_failure: false
```

### 6.3 Jenkins Pipeline

```groovy
pipeline {
    agent {
        docker { image 'mcr.microsoft.com/playwright:v1.50.0-jammy' }
    }
    stages {
        stage('Install') {
            steps {
                dir('e2e-tests') { sh 'pnpm install --frozen-lockfile' }
            }
        }
        stage('Smoke Test') {
            steps {
                dir('e2e-tests') { sh 'npx playwright test --project=smoke-chromium' }
            }
        }
        stage('Regression Test') {
            when { branch 'main' }
            steps {
                dir('e2e-tests') {
                    sh 'npx playwright test --project=regression-chromium --project=regression-firefox'
                }
            }
        }
    }
    post {
        always {
            archiveArtifacts artifacts: 'e2e-tests/playwright-report/**'
        }
        failure {
            echo "E2E 测试失败，请查看报告"
        }
    }
}
```

---

## 7. 医疗业务测试场景

### 7.1 报告状态流转测试

完整状态机：

```
                 ┌───────── 退回 ─────────┐
                 │                        │
                 ▼                        │
[草稿] ──提交审核──→ [待审核] ──审核通过──→ [已审核] ──发布──→ [已发布]
  │                   │                      │
  └──── 删除 ────→ [已删除]    [已作废] ◄─── 作废
```

### 7.2 角色权限验证

| 场景 | 角色 | 操作 | 预期 |
|------|------|------|------|
| 医生编辑自己的报告 | 医生 | 编辑自己创建的报告 | 允许 |
| 医生编辑他人报告 | 医生 | 编辑他人创建的报告 | 拒绝/不可见 |
| 医生审核报告 | 医生 | 尝试审核操作 | 无审核按钮或操作被拒 |
| 审核医生编辑报告 | 审核医生 | 尝试编辑报告内容 | 编辑按钮不可用 |

---

## 8. 测试报告与度量

### 8.1 报告类型

| 报告类型 | 用途 | 输出格式 |
|----------|------|---------|
| **HTML Report** | 本地调试/快速查看 | HTML |
| **JUnit XML** | CI 集成/历史趋势 | XML |
| **Allure Report** | 详细分析/历史对比 | HTML |

### 8.2 关键度量指标

| 指标 | 目标值 | 计算方式 |
|------|--------|---------|
| 冒烟测试通过率 | 100% | 冒烟用例通过数 / 总数 |
| 回归测试通过率 | ≥95% | 回归用例通过数 / 总数 |
| 用例执行时间 | 冒烟≤5min / 回归≤30min | 从开始到结束的总时长 |
| 缺陷发现率 | 每迭代≥5 个 | 测试发现的 Bug 数 |
| 自动化覆盖率 | P0 100% / P1≥90% | 已自动化场景 / 总场景 |

---

## 9. 本地开发指南

### 9.1 环境准备

```bash
# 安装 Node.js (≥18 LTS)
# 安装 pnpm
npm install -g pnpm

# 克隆项目
cd e2e-tests

# 安装依赖
pnpm install

# 安装 Playwright 浏览器
npx playwright install
```

### 9.2 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件
BASE_URL=http://localhost:8080
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
```

### 9.3 常用命令

```bash
# 运行冒烟测试
pnpm test:smoke

# 运行回归测试
pnpm test:regression

# 运行全部测试
pnpm test:all

# 列出所有测试用例
pnpm test:list

# 查看 HTML 报告
pnpm report:html

# 查看 Allure 报告
pnpm report:allure

# AI 生成测试用例
pnpm ai:generate

# AI 扩展现有脚本
pnpm ai:extend

# 启动 AI 测试管理 UI
pnpm ui
```

### 9.4 调试技巧

```bash
# 单文件调试
npx playwright test tests/smoke/login.spec.ts

# 单个测试用例调试（通过名称匹配）
npx playwright test -g "医生登录系统"

# 有头模式调试（可视化）
npx playwright test --headed

# 慢速模式（逐步执行）
npx playwright test --debug

# 追踪模式（记录完整 trace）
npx playwright test --trace on
```

---

## 10. 风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| **UI 频繁变更** | 定位器失效，用例失败 | 1. 使用 `data-testid` 稳定定位<br>2. AI 定位器自愈<br>3. 建立 UI 变更通知机制 |
| **测试数据污染** | 用例执行结果不可靠 | 1. 每用例独立数据准备<br>2. 执行后自动清理<br>3. 数据库快照恢复 |
| **环境不稳定** | 误报率高 | 1. 失败自动重试 (CI 2 次)<br>2. 智能等待策略<br>3. 环境健康检查 |
| **执行时间过长** | CI 流水线阻塞 | 1. 并发执行<br>2. 用例分级 (冒烟/回归)<br>3. 按需执行标签 |

---

## 11. 总结

本技术方案采用 **Playwright + TypeScript + AI 辅助** 的现代化自动化测试架构，具有以下优势：

1. **高效执行**：冒烟测试≤5 分钟，回归测试≤30 分钟
2. **稳定可靠**：Page Object 模式 + 稳定定位策略 + 失败重试
3. **AI 赋能**：用例生成、脚本生成、失败分析、定位器自愈
4. **CI/CD 集成**：GitLab CI / Jenkins 无缝对接，质量门禁
5. **可维护性**：TypeScript 类型安全、清晰的项目结构、完善的文档

通过本方案的实施，可显著提升测试效率，降低人为遗漏风险，为医院体检报告管理系统的质量保驾护航。

---

*文档版本：v1.0*  
*最后更新：2026 年 3 月 15 日*
